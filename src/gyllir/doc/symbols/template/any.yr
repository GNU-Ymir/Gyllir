in any;

use std::stream;

@final
pub class TemplateAnyParam over TemplateParam {
    pub let content : [c8] = [];

    pub self (content : [c8])
        with content = content
    {}

    impl Streamable {
        pub over toStream (self, dmut stream : &StringStream) {
            stream:.write (self.content);
        }
    }

}
