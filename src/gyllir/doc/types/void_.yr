mod gyllir::doc::types::void_;

import gyllir::doc::types::base;
import std::io;

/**
 * A void type
 */
pub class VoidType over Type {

    /**
     * A new void type (always immutable)
     */
    pub self () {}

    impl Streamable;
}
