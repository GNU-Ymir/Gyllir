in option;

use std::stream;

@final
pub class OptionType over Type {

    // The inner type of the option
    pub let child : &Type;

    pub self (mutability : [c8], inner : &Type)
        with super (mutability-> mutability)
        , child = inner
    {}

    impl Streamable {
        pub over toStream (self, dmut stream : &StringStream) {
            stream:.write ("Option (", self.child, ")");
        }
    }
    
}
