in runner;

use std::{io, env, stream, config::_, fs::{path, errors}, concurrency::process};
use gyllir::{repo::_, utils::_, config::_};


pub record RepoRunner {

    // The current working directory
    let _cwd: Path;

    // The command line options
    let _args: &Config;

    // The package configuration
    let _cfg: GyllirPackageConfiguration;

    // The target whose artifact is executed
    let _target: Target;

    // True iif the runner is configured to run unittests
    let _forUnitTest: bool;

    /**
     * @params:
     *    - cfg: the configuration of the repo
     *    - target: the target whose artifact is executed
     * */
    pub self (path: Path, args: &Config, cfg: GyllirPackageConfiguration, target: Target, forUnitTest: bool)
        with _cwd = path
        , _args = args
        , _cfg = cfg
        , _target = target
        , _forUnitTest = forUnitTest
    {}

    /**
     * Run the build executable
     * @returns: the exit code of the executed artifact
     * @info: the caller is the one exiting, so that 'gyllir test' can run the tests of every target
     *   before failing on the first one that did not pass
     * */
    pub fn run(self)-> i32
        throws AssertError, FsError
    {
        let output = self._target.outputName();
        if (self._forUnitTest) {
            utils::log::info("Executing", "tests ", output, " v", self._cfg.version[]);
            std::io::println("\n");
            let dmut subProc = copy SubProcess("./" ~ output ~ ".test", [], cwd-> self._cwd);
            subProc:.start(redirect-> false);

            self.exitCode(subProc:.wait())
        } else {
            if !self._target.type.isExecutable() {
                throw copy AssertError("target '" ~ self._target.name ~ "' is a library, 'gyllir run' only runs an executable target");
            }

            let mut options: [[c8]] = [];
            if let a: &Array = self._args {
                for i in a[] if let Str(value-> v) = i {
                    options ~= [v];
                }
            }

            utils::log::info("Executing", "project ", output, " v", self._cfg.version[]);
            std::io::println("\n");
            let dmut subProc = copy SubProcess("./" ~ output, options, cwd-> self._cwd);
            subProc:.start(redirect-> false);

            self.exitCode(subProc:.wait())
        }
    }

    /**
     * The exit code of a finished process
     * @params:
     *    - status: the raw status 'SubProcess::wait' returns, straight out of 'waitpid'
     * @returns: the code the artifact exited with, or '128 + <signal>' when a signal killed it, the
     *    way a shell reports it
     * @info: the exit code sits in the second byte of that status, so passing it as is to 'exit'
     *    truncates every code to zero and a failing artifact reports a success
     * */
    prv fn exitCode(self, status: i32)-> i32 {
        let signal = status & 0x7f;
        if (signal != 0) {
            return 128 + signal;
        }

        (status >> 8) & 0xff
    }

}
