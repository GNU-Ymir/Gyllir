in builder;

use gyllir::config::_;
use gyllir::utils::_;

/**
 * Build a repository
 */
pub record RepoBuilder {

    // The configuration of the repo to build
    let _cfg : GyllirPackageConfiguration;

    /**
     * @params:
     *    - cfg: the configuration of the repo
     * */
    pub self (cfg : GyllirPackageConfiguration)
        with _cfg = cfg
    {}

    /**
     * Build the repository
     * */
    pub fn run (self) {
    }

}
