use gyllir::doc::symbols::template_::any;

use std::stream;

__test raw_template_param_rendering {
    let p = copy TemplateAnyParam("T");
    let dmut stream = copy StringStream();
    stream:.write(p);

    assert(stream[] == "T", "unexpected rendering for a raw template param");
}
